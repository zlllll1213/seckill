# 全栈秒杀系统 — 优化方案

> 项目：Spring Boot 3.2.5 + Vue 3 / Element Plus + Electron  
> 核心流程：Redis Lua 原子扣库存 → RabbitMQ 异步建单 → 前端轮询结果

---

## 目录

1. [前端美观度 (Aesthetics)](#一前端美观度-aesthetics)
2. [内存与性能 (Memory & Performance)](#二内存与性能-memory--performance)
3. [安全性 (Security)](#三安全性-security)
4. [优化优先级总表](#四优化优先级总表)
5. [文件改动清单](#五具体文件改动清单)

---

## 一、前端美观度 (Aesthetics)

### 1.1 当前问题

| # | 问题 | 位置 | 严重度 |
|---|------|------|--------|
| A1 | 全白背景 + Element Plus 默认蓝色，无品牌主色调 | 所有页面 | ⭐⭐ |
| A2 | 登录/注册页过于简陋，无品牌感、无背景装饰 | `LoginView.vue` / `RegisterView.vue` | ⭐⭐ |
| A3 | 查询列表无骨架屏 — `v-loading` 全屏遮罩遮挡内容 | `ProductListView.vue` / `OrderListView.vue` 等 | ⭐⭐ |
| A4 | 无页面切换过渡动画 — router-view 生硬跳转 | `App.vue` | ⭐ |
| A5 | 商品图片无懒加载、无加载失败 fallback | `ProductCard.vue` / `ProductDetailView.vue` | ⭐⭐ |
| A6 | 秒杀成功无庆祝反馈 — 仅一个 el-result 图标 | `SeckillView.vue` | ⭐⭐ |
| A7 | 空状态指引弱 — 只有 "暂无商品/订单" 无操作引导 | 所有列表页 | ⭐ |
| A8 | 导航栏无滚动阴影 — 粘性定位后与内容无区分 | `NavBar.vue` | ⭐ |
| A9 | 管理员表格区无卡片容器 — 表格直接贴边 | `admin/*.vue` | ⭐ |
| A10 | 无深色模式支持 | 全局 | ⭐ |
| A11 | 选择器/日期选择器等未统一主题，使用 Element Plus 默认样式 | 各 dialog | ⭐ |
| A12 | 列表页无搜索/筛选功能 — 只能全量翻页 | `ProductListView.vue` | ⭐⭐ |

### 1.2 优化方案

#### a) 品牌主题色 — 覆盖 Element Plus CSS 变量

新建 `src/styles/element-variables.scss`：

```scss
// 秒杀系统主题色 — 红金配
:root {
  --el-color-primary: #e64545;
  --el-color-primary-light-3: #f06262;
  --el-color-primary-light-5: #f89898;
  --el-color-primary-light-7: #fcd5d5;
  --el-color-primary-dark-2: #c62828;
  --el-border-radius-base: 8px;
}
```

在 `vite.config.js` 中配置 `ElementPlusResolver` 启用 scss：

```javascript
ElementPlusResolver({ importStyle: 'sass' }),
// 并设置 scss 变量文件路径
css: {
  preprocessorOptions: {
    scss: {
      additionalData: `@use "@/styles/element-variables.scss" as *;`
    }
  }
}
```

#### b) 骨架屏替代 v-loading

```vue
<el-skeleton :loading="loading" animated>
  <template #template>
    <div class="grid">
      <el-skeleton-item v-for="i in 8" :key="i" variant="card" style="height:300px" />
    </div>
  </template>
  <template #default>
    <div class="grid">
      <ProductCard v-for="p in products" :key="p.id" :product="p" />
    </div>
  </template>
</el-skeleton>
```

#### c) 路由过渡动画

`App.vue`：

```vue
<router-view v-slot="{ Component }">
  <transition name="fade-slide" mode="out-in">
    <component :is="Component" />
  </transition>
</router-view>
```

```css
.fade-slide-enter-active, .fade-slide-leave-active {
  transition: all 0.25s ease;
}
.fade-slide-enter-from { opacity: 0; transform: translateY(12px); }
.fade-slide-leave-to   { opacity: 0; transform: translateY(-8px); }
```

#### d) 图片懒加载 + fallback

```vue
<el-image
  :src="product.imageUrl"
  :alt="product.name"
  lazy
>
  <template #error>
    <div class="img-fallback">📷 暂无图片</div>
  </template>
</el-image>
```

#### e) 秒杀成功庆祝效果

集成轻量 `canvas-confetti` (≈3KB gzip) 或在成功时播放 CSS 动画序列。

#### f) 登录/注册页美化

- 渐变背景 + 抽象几何装饰（纯 CSS）
- 卡片增加微投影 + backdrop-blur
- 增加 Logo 视觉层级

#### g) 导航栏滚动阴影

```vue
<script setup>
import { ref, onMounted, onUnmounted } from 'vue'
const scrolled = ref(false)
const onScroll = () => { scrolled.value = window.scrollY > 0 }
onMounted(() => window.addEventListener('scroll', onScroll))
onUnmounted(() => window.removeEventListener('scroll', onScroll))
</script>
<template>
  <el-header :class="{ 'navbar-scrolled': scrolled }">...</el-header>
</template>
```

```css
.navbar-scrolled { box-shadow: 0 2px 8px rgba(0,0,0,0.06); }
```

#### h) 深色模式

使用 `@vueuse/core` 的 `useDark` / `useToggle`，配合 Element Plus `dark` CSS 变量覆盖。

---

## 二、内存与性能 (Memory & Performance)

### 2.1 当前问题

| # | 问题 | 位置 | 严重度 |
|---|------|------|--------|
| M1 | Element Plus 全量引入样式 — 部分冗余 | `main.js` | ⭐⭐⭐ |
| M2 | 秒杀轮询 `setInterval` 永不退避 — 长时间挂机持续请求 | `SeckillView.vue` `startPolling()` | ⭐⭐ |
| M3 | 倒计时组件 1s 间隔永不暂停 — 页面不可见时仍运行 | `CountdownTimer.vue` | ⭐⭐ |
| M4 | 商品列表无虚拟滚动 — 大数据量时 DOM 节点爆炸 | `ProductListView.vue` | ⭐ |
| M5 | `ref()` 包装大型数组 — 全量深度响应式代理开销 | 多个列表视图 | ⭐⭐ |
| M6 | 图片无懒加载 — 不可见图片也发起 HTTP 请求 | `ProductCard.vue` | ⭐⭐ |
| M7 | `Promise.allSettled` 无 Loading 分步 — 仪表盘 3 请求串行阻塞 UI | `DashboardView.vue` | ⭐ |
| M8 | 无 KeepAlive 缓存 — 列表页切换回来重新请求 | `App.vue` + router | ⭐ |

### 2.2 优化方案

#### a) 开启 Element Plus 按需加载

当前 `vite.config.js` 已有 `unplugin-vue-components` + `unplugin-auto-import` 的 `ElementPlusResolver`。还需：

```diff
- import 'element-plus/dist/index.css'  // 从 main.js 移除
+ // 改用 ElementPlusResolver({ importStyle: 'sass' }) 按需导入样式
```

#### b) 秒杀轮询 — 指数退避

```javascript
function startPolling() {
  let delay = 1000
  const poll = () => {
    pollTimer = setTimeout(async () => {
      try {
        const res = await seckillApi.getResult(route.params.id)
        const { status, orderId: oid } = res.data
        if (status === 'success' || status === 'fail') {
          resultStatus.value = status
          orderId.value = oid
          return
        }
      } catch { /* ignore */ }
      delay = Math.min(delay * 1.5, 10000)  // 最大 10s
      poll()
    }, delay)
  }
  poll()
}
```

#### c) 倒计时 — Page Visibility API + requestAnimationFrame

```javascript
let rafId
function tick() {
  const diff = Math.floor((new Date(props.targetTime) - Date.now()) / 1000)
  remaining.value = Math.max(0, diff)
  if (remaining.value <= 0) { emit('ended'); return }
  if (document.visibilityState === 'visible') {
    rafId = requestAnimationFrame(tick)
  } else {
    rafId = setTimeout(tick, 1000)  // 隐藏时降级
  }
}
onMounted(tick)
onUnmounted(() => cancelAnimationFrame(rafId) || clearTimeout(rafId))
```

#### d) 大型列表 shallowRef

```javascript
import { shallowRef, triggerRef } from 'vue'
const products = shallowRef([])

// 更新时直接替换数组（不追踪内部元素）
products.value = res.data.records || []
triggerRef(products)  // 手动触发
```

#### e) 路由 KeepAlive

```vue
<router-view v-slot="{ Component, route }">
  <keep-alive :include="['products', 'orders']">
    <component :is="Component" :key="route.fullPath" />
  </keep-alive>
</router-view>
```

> 注意：KeepAlive 下的页面 `onMounted` 需改为 `onActivated`。

#### f) 定时器清理清单

每个使用 `setInterval` / `setTimeout` / `requestAnimationFrame` 的组件，必须在 `onUnmounted` 中清理：

| 组件 | 定时器变量 | 清理方式 |
|------|-----------|---------|
| `CountdownTimer.vue` | `timer` / `rafId` | `clearInterval / cancelAnimationFrame` |
| `SeckillView.vue` | `pollTimer` | `clearTimeout` |

---

## 三、安全性 (Security)

### 3.1 当前问题

| # | 问题 | 位置 | 严重度 |
|---|------|------|--------|
| S1 | **JWT 存储在 localStorage** — XSS 可窃取，无法设置 httpOnly | `stores/user.js` + `api/index.js` | 🔴🔴🔴 |
| S2 | **无登录/秒杀接口限流** — 可暴力破解 / 刷单 | `UserController.java` / `SeckillController.java` | 🔴🔴🔴 |
| S3 | **JWT secret 硬编码在 application.yml** — 非外部化密钥 | `application.yml` | 🔴🔴 |
| S4 | **无 CORS 白名单** — 默认可能允许非预期来源 | `SecurityConfig.java` | 🔴🔴 |
| S5 | **无 Content-Security-Policy** — XSS 攻击面敞开 | `index.html` / 后端响应头 | 🔴🔴 |
| S6 | **密码强度校验弱** — 前端仅 min-length:6，后端无复杂度规则 | `RegisterView.vue` / `RegisterRequest.java` | 🔴🔴 |
| S7 | **注册邮箱标注"选填"但后端 `@NotBlank`** — 前后端不一致 | `RegisterView.vue` vs `RegisterRequest.java` | 🔴 |
| S8 | **商品 `imageUrl` / `description` 无输出编码** — 存储型 XSS 入口 | `ProductManageView.vue` | 🔴🔴 |
| S9 | **秒杀活动进行中可修改价格** — 价格一致性问题 | `ActivityServiceImpl.java` | 🔴🔴 |
| S10 | **MyBatis-Plus SQL 日志始终开启** — 生产环境泄露查询 | `application.yml` | 🔴🔴 |
| S11 | **无 refresh token 机制** — 24h 单 JWT 无法轮换 | `JwtUtils.java` | 🔴 |
| S12 | **登出仅清 localStorage** — JWT 仍有效直至过期 | `stores/user.js` | 🔴 |
| S13 | **Docker MySQL 端口映射到宿主机** — 外部可访问 | `docker-compose.yml` | 🔴 |
| S14 | **无请求日志审计** — 无法追溯恶意请求 | 全局 | 🔴 |

### 3.2 优化方案

#### a) JWT 迁移至 httpOnly Cookie (关键 🔴🔴🔴)

当前 `localStorage + Bearer header` 在 XSS 面前完全暴露。

**后端改动** — `UserController.java`：

```java
@PostMapping("/login")
public Result<Void> login(@Valid @RequestBody LoginRequest req, HttpServletResponse response) {
    String token = userService.login(req);
    Cookie cookie = new Cookie("jwt", token);
    cookie.setHttpOnly(true);
    cookie.setSecure(true);       // HTTPS 下启用
    cookie.setPath("/");
    cookie.setMaxAge((int)(expiration / 1000));
    cookie.setAttribute("SameSite", "Strict");
    response.addCookie(cookie);
    return Result.success();      // 不再返回 token body
}
```

**JWT 过滤器** — `JwtAuthenticationFilter.java` 增加从 Cookie 读取的逻辑：

```java
private String extractToken(HttpServletRequest request) {
    String header = request.getHeader("Authorization");
    if (StringUtils.hasText(header) && header.startsWith("Bearer ")) {
        return header.substring(7);
    }
    // 兜底：从 Cookie 读取
    if (request.getCookies() != null) {
        for (Cookie c : request.getCookies()) {
            if ("jwt".equals(c.getName())) return c.getValue();
        }
    }
    return null;
}
```

**前端改动**：

```javascript
// api/index.js — 移除手动拼接 Authorization
http.interceptors.request.use(config => {
  // Cookie 由浏览器自动携带，无需任何操作
  return config
})

// stores/user.js — 移除 token 存取逻辑
// 只保留 username 和 role 的本地缓存
```

> **过渡方案**（如暂不改后端）：将 token 存入 sessionStorage + 增加 CSP 严格限制。

#### b) 接口限流 (关键 🔴🔴🔴)

**方案一**：Bucket4j 轻量限流

```xml
<dependency>
    <groupId>com.github.vladimir-bukhtoyarov</groupId>
    <artifactId>bucket4j-core</artifactId>
    <version>8.7.0</version>
</dependency>
```

```java
// 限流过滤器
@Component
public class RateLimitFilter extends OncePerRequestFilter {
    private final Map<String, Bucket> buckets = new ConcurrentHashMap<>();

    @Override
    protected void doFilterInternal(HttpServletRequest request, HttpServletResponse response, FilterChain chain)
            throws ServletException, IOException {
        String key = request.getRemoteAddr() + ":" + request.getRequestURI();

        Bucket bucket = buckets.computeIfAbsent(key, k ->
                Bucket.builder()
                        .addLimit(Bandwidth.simple(10, Duration.ofSeconds(1)))  // 10 req/s
                        .build());

        if (bucket.tryConsume(1)) {
            chain.doFilter(request, response);
        } else {
            response.setStatus(429);
            response.getWriter().write("{\"code\":429,\"message\":\"请求过于频繁\"}");
        }
    }
}
```

**方案二**：Nginx 网关层限流（推荐生产用）：

```nginx
limit_req_zone $binary_remote_addr zone=seckill:10m rate=5r/s;
limit_req_zone $binary_remote_addr zone=login:10m rate=3r/m;

location /api/seckill/ { limit_req zone=seckill; }
location /api/auth/login { limit_req zone=login; }
```

**需要限流的接口**：

| 接口 | 限流建议 | 理由 |
|------|---------|------|
| `POST /api/auth/login` | 每 IP 5 次/分钟 | 防暴力破解 |
| `POST /api/auth/register` | 每 IP 3 次/分钟 | 防批量注册 |
| `POST /api/seckill/{id}` | 每用户 1 次/秒 | 防刷单 |
| `GET /api/seckill/result/{id}` | 每用户 5 次/秒 | 防过度轮询 |

#### c) CSP 安全头

**后端** — `SecurityConfig.java`：

```java
.headers(h -> h
    .contentSecurityPolicy(csp -> csp
        .policyDirectives(
            "default-src 'self'; " +
            "script-src 'self'; " +
            "style-src 'self' 'unsafe-inline'; " +
            "img-src 'self' https: data:; " +
            "connect-src 'self' http://localhost:8080; " +
            "font-src 'self' data:; " +
            "frame-ancestors 'none'"
        ))
    .frameOptions(f -> f.deny())
    .referrerPolicy(r -> r.policy(ReferrerPolicyHeaderWriter.ReferrerPolicy.STRICT_ORIGIN_WHEN_CROSS_ORIGIN))
)
```

**前端** — `index.html`：

```html
<meta http-equiv="Content-Security-Policy" content="default-src 'self'; ...">
```

#### d) JWT Secret 环境变量化

```yaml
# application.yml
jwt:
  secret: ${JWT_SECRET}
  expiration: ${JWT_EXPIRATION:86400000}
```

```yaml
# application-dev.yml（加入 .gitignore）
jwt:
  secret: dev-secret-key-not-for-production
```

#### e) CORS 白名单

```java
.cors(c -> c.configurationSource(request -> {
    CorsConfiguration config = new CorsConfiguration();
    config.setAllowedOrigins(List.of(
        "http://localhost:5173",
        "app://."   // Electron
    ));
    config.setAllowedMethods(List.of("GET","POST","PUT","DELETE","OPTIONS"));
    config.setAllowedHeaders(List.of("*"));
    config.setAllowCredentials(true);
    config.setMaxAge(3600L);
    return config;
}))
```

#### f) 密码强度加强

**后端** — `RegisterRequest.java`：

```java
@NotBlank(message = "密码不能为空")
@Pattern(regexp = "^(?=.*[A-Za-z])(?=.*\\d)[A-Za-z\\d@$!%*#?&]{8,20}$",
         message = "密码需包含字母和数字，长度8-20位")
private String password;
```

**前端** — `RegisterView.vue`：

```javascript
const rules = {
  password: [
    { required: true, message: '请输入密码', trigger: 'blur' },
    { pattern: /^(?=.*[A-Za-z])(?=.*\d).{8,20}$/, message: '8-20位，需包含字母和数字', trigger: 'blur' }
  ]
}
```

#### g) 注册邮箱前后端对齐

```java
// RegisterRequest.java — 改为非必填
@Email(message = "邮箱格式不正确")
private String email;
```

```vue
<!-- 或前端改为 *必填 -->
```

#### h) XSS 输出编码

所有用户输入文本字段在前端展示时使用 Vue 默认文本插值 `{{ }}` 已自动转义。但为防止存储型 XSS，在后端入库前做 HTML 转义：

```java
// 工具类 HtmlSanitizer.java
public static String sanitize(String input) {
    if (input == null) return null;
    return input.replace("&", "&amp;")
                .replace("<", "&lt;")
                .replace(">", "&gt;")
                .replace("\"", "&quot;")
                .replace("'", "&#x27;");
}
```

在 `ProductServiceImpl` / `ActivityServiceImpl` 的 create/update 中调用。

#### i) 生产关闭 SQL 日志

```yaml
# application-prod.yml
mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.nologging.NoLoggingImpl
```

通过 `spring.profiles.active=prod` 切换。

#### j) 活动进行中禁止改价

```java
// ActivityServiceImpl.updateActivity()
if (existing.getStatus() == 1) {
    activity.setSeckillPrice(existing.getSeckillPrice()); // 锁定价格
    activity.setStock(existing.getStock());               // 锁定库存
}
```

#### k) Docker 端口安全

```yaml
# docker-compose.yml
services:
  mysql:
    ports:
      - "127.0.0.1:3307:3306"   # 仅本地访问
  redis:
    ports:
      - "127.0.0.1:6379:6379"
  rabbitmq:
    ports:
      - "127.0.0.1:5672:5672"
      - "127.0.0.1:15672:15672"
```

#### l) 请求日志审计

```java
// 在 SecurityConfig 或单独过滤器
@Component
public class AuditLogFilter extends OncePerRequestFilter {
    @Override
    protected void doFilterInternal(HttpServletRequest request, ...) {
        String userId = "anonymous";
        try {
            userId = SecurityContextHolder.getContext().getAuthentication().getName();
        } catch (Exception ignored) {}
        log.info("[AUDIT] {} {} {} from {}",
            userId, request.getMethod(), request.getRequestURI(), request.getRemoteAddr());
        chain.doFilter(request, response);
    }
}
```

---

## 四、优化优先级总表

| 优先级 | 编号 | 事项 | 预估工时 |
|--------|------|------|----------|
| **P0** | S1 | JWT 迁移至 httpOnly Cookie | 2h |
| **P0** | S2 | 登录/秒杀接口限流 | 2h |
| **P0** | S4+S5 | CORS 白名单 + CSP 安全头 | 0.5h |
| **P1** | A1 | 品牌主题色覆盖 | 1h |
| **P1** | A3 | 骨架屏替代 v-loading | 1h |
| **P1** | M2 | 秒杀轮询指数退避 | 0.5h |
| **P1** | M3 | 倒计时 Visibility 感知 | 0.5h |
| **P1** | S6 | 密码强度加强（前后端） | 0.5h |
| **P1** | S8 | XSS 输入过滤 | 1h |
| **P1** | S9 | 活动中锁定价格 | 0.3h |
| **P1** | S10 | 生产关闭 SQL 日志 | 0.2h |
| **P2** | A2 | 登录/注册页美化 | 1.5h |
| **P2** | A4 | 路由过渡动画 | 0.5h |
| **P2** | A5 | 图片懒加载 + fallback | 0.5h |
| **P2** | A6 | 秒杀成功庆祝效果 | 1h |
| **P2** | M5 | `shallowRef` 优化大列表 | 0.3h |
| **P2** | S3 | JWT Secret 环境变量化 | 0.3h |
| **P2** | S7 | 注册邮箱前后端对齐 | 0.2h |
| **P2** | S11+S12 | refresh token + 服务端登出 | 2h |
| **P3** | A8 | 导航栏滚动阴影 | 0.3h |
| **P3** | A9 | 管理后台卡片化 | 0.5h |
| **P3** | A10 | 深色模式 | 2h |
| **P3** | M8 | 路由 KeepAlive | 0.3h |
| **P3** | S13 | Docker 端口绑定本地 | 0.2h |
| **P3** | S14 | 请求日志审计 | 0.5h |

---

## 五、具体文件改动清单

### 前端 (Vue 3 / Electron)

| 文件 | 改动内容 | 优先级 |
|------|---------|--------|
| `frontend/src/main.js` | 移除全量 `element-plus/dist/index.css` 导入 | P1 |
| `frontend/src/styles/element-variables.scss` | **新建** — 品牌主题色变量 | P1 |
| `frontend/src/api/index.js` | 移除 Bearer token 拼接逻辑 | P0 |
| `frontend/src/stores/user.js` | 移除 token 存取，保留 username/role | P0 |
| `frontend/src/views/SeckillView.vue` | 轮询指数退避 + 成功庆祝动画 | P1 |
| `frontend/src/components/CountdownTimer.vue` | Visibility API + rAF 优化 | P1 |
| `frontend/src/components/ProductCard.vue` | el-image 懒加载 + error fallback | P2 |
| `frontend/src/components/NavBar.vue` | 滚动阴影 + （可选）深色模式切换 | P2 |
| `frontend/src/views/LoginView.vue` | 视觉重构 — 渐变背景/装饰/品牌 | P2 |
| `frontend/src/views/RegisterView.vue` | 视觉重构 + 密码强度正则增强 | P1 |
| `frontend/src/views/ProductListView.vue` | 骨架屏 + shallowRef + 搜索筛选 | P1 |
| `frontend/src/views/OrderListView.vue` | 骨架屏 | P1 |
| `frontend/src/views/OrderDetailView.vue` | 骨架屏 | P1 |
| `frontend/src/views/admin/DashboardView.vue` | 卡片包装 + 主题统一 | P2 |
| `frontend/src/views/admin/ProductManageView.vue` | 卡片包装 | P2 |
| `frontend/src/views/admin/ActivityManageView.vue` | 卡片包装 | P2 |
| `frontend/src/views/admin/OrderManageView.vue` | 卡片包装 | P2 |
| `frontend/src/App.vue` | 路由过渡动画 + KeepAlive | P2 |
| `frontend/vite.config.js` | ElementPlusResolver 开启 importStyle: 'sass' | P1 |
| `frontend/index.html` | 增加 CSP meta 标签 | P0 |

### 后端 (Spring Boot)

| 文件 | 改动内容 | 优先级 |
|------|---------|--------|
| `backend/src/main/resources/application.yml` | JWT secret 环境变量化 + 拆分 prod profile | P1 |
| `backend/src/main/resources/application-prod.yml` | **新建** — 关闭 SQL 日志 | P1 |
| `backend/.../config/SecurityConfig.java` | 增加 CORS 白名单 + CSP 头 + 限流过滤器注册 | P0 |
| `backend/.../config/JwtAuthenticationFilter.java` | 增加从 Cookie 读取 JWT 的逻辑 | P0 |
| `backend/.../config/JwtUtils.java` | 增加 refresh token 方法（可选） | P2 |
| `backend/.../config/RateLimitFilter.java` | **新建** — 接口限流过滤器 | P0 |
| `backend/.../user/controller/UserController.java` | httpOnly Cookie 写入 + 登录限流 | P0 |
| `backend/.../user/dto/RegisterRequest.java` | 密码正则 + email 改为非必填 | P1 |
| `backend/.../user/service/impl/UserServiceImpl.java` | 密码强度校验逻辑 | P1 |
| `backend/.../seckill/controller/SeckillController.java` | 秒杀接口限流 | P0 |
| `backend/.../seckill/service/impl/SeckillServiceImpl.java` | 活动进行中价格断言 | P1 |
| `backend/.../activity/service/impl/ActivityServiceImpl.java` | update 时锁定进行中活动的价格/库存 | P1 |
| `backend/.../product/service/impl/ProductServiceImpl.java` | XSS 过滤 description/name | P1 |
| `backend/.../activity/service/impl/ActivityServiceImpl.java` | XSS 过滤 name | P1 |
| `backend/.../common/GlobalExceptionHandler.java` | 增加限流异常处理 | P0 |
| `backend/.../common/AuditLogFilter.java` | **新建** — 请求审计日志 | P3 |
| `backend/pom.xml` | 增加 bucket4j / resilience4j 依赖 | P0 |

### 基础设施

| 文件 | 改动内容 | 优先级 |
|------|---------|--------|
| `docker-compose.yml` | 端口映射改为 `127.0.0.1` 绑定 | P3 |
| `docker-compose.yml` | 添加 `.env` 文件引用分离密码 | P2 |
| `frontend/electron/main.js` | 增加 `webPreferences.webSecurity = true` | P1 |
| `frontend/electron-builder.yml` | 添加证书签名配置 | P2 |

---

## 六、优化后检验清单

### 美观度验收
- [ ] 所有页面使用统一的品牌红色调
- [ ] 列表页有骨架屏加载过渡
- [ ] 页面切换有平滑过渡动画
- [ ] 秒杀成功有正向反馈（动画/提示）
- [ ] 登录/注册页有品牌视觉设计
- [ ] 图片加载失败有优雅降级
- [ ] 导航栏滚动时有阴影分隔

### 内存与性能验收
- [ ] Element Plus 样式按需加载
- [ ] 秒杀轮询 60s 后不超过 10s 一次
- [ ] 页面隐藏时倒计时不频繁渲染
- [ ] 大列表使用 shallowRef 减少响应式开销
- [ ] 所有定时器在组件卸载时正确清理
- [ ] 图片延迟加载

### 安全验收
- [ ] JWT 通过 httpOnly Cookie 传递
- [ ] 登录接口每分钟 ≤5 次
- [ ] 秒杀接口每秒 ≤1 次
- [ ] CSP 安全头已配置
- [ ] CORS 已限定来源白名单
- [ ] 密码要求 8-20 位字母+数字
- [ ] 生产环境 SQL 日志已关闭
- [ ] Docker 端口未暴露到公网
- [ ] JWT Secret 未硬编码
- [ ] XSS 输入已过滤
