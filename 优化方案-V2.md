# 全栈秒杀系统 — 优化方案 V2（深度架构与可靠性）

> 前提：V1 的表面层优化（美观度 / 内存 / 基础安全）已大量落地  
> V2 聚焦：**架构韧性 → 可观测性 → 数据一致性 → 运维就绪 → 测试体系**

---

## 目录

1. [架构韧性 (Architecture Resilience)](#一架构韧性-architecture-resilience)
2. [可观测性 (Observability)](#二可观测性-observability)
3. [数据一致性 (Data Integrity)](#三数据一致性-data-integrity)
4. [API 设计深化](#四api-设计深化)
5. [后端性能深化](#五后端性能深化)
6. [前端体验深化](#六前端体验深化)
7. [测试体系](#七测试体系)
8. [运维就绪 (Operations)](#八运维就绪-operations)
9. [安全纵深](#九安全纵深)
10. [优化优先级总表](#十优化优先级总表)

---

## 一、架构韧性 (Architecture Resilience)

### 1.1 MQ 发布确认缺失 (关键 🔴🔴🔴)

**问题**：`SeckillProducer.sendSeckillMessage()` 调用 `rabbitTemplate.convertAndSend()` 无确认回调。若 RabbitMQ 不可达，消息静默丢失——Redis 已扣库存，订单永不创建。

```java
// 当前代码 — 无确认
rabbitTemplate.convertAndSend(exchangeName, routingKey, msg);
```

**方案**：

```java
// RabbitMQConfig.java — 开启 publisher confirms + returns
@Bean
public RabbitTemplate rabbitTemplate(ConnectionFactory connectionFactory) {
    RabbitTemplate template = new RabbitTemplate(connectionFactory);
    template.setMessageConverter(messageConverter());
    template.setConfirmCallback((correlationData, ack, cause) -> {
        if (!ack) {
            log.error("[MQ] 消息发送未确认：correlationId={}, cause={}",
                    correlationData != null ? correlationData.getId() : "null", cause);
            // 触发补偿 — 将消息持久化到 DB 或写入死信
        }
    });
    template.setReturnsCallback(returned -> {
        log.error("[MQ] 消息路由失败：replyCode={}, replyText={}, exchange={}, routingKey={}",
                returned.getReplyCode(), returned.getReplyText(),
                returned.getExchange(), returned.getRoutingKey());
        // 补偿逻辑
    });
    return template;
}
```

```java
// SeckillProducer.java — 改造后
public void sendSeckillMessage(SeckillMessage msg) {
    CorrelationData correlationData = new CorrelationData(
            msg.getActivityId() + ":" + msg.getUserId());
    rabbitTemplate.convertAndSend(exchangeName, routingKey, msg, correlationData);
}
```

**补偿策略**（确认失败时）：

| 策略 | 方案 | 适用场景 |
|------|------|---------|
| A | 将消息持久化到 `seckill_fallback` DB 表 + 定时任务重投 | 可靠性最强 |
| B | 写入 Redis `seckill:fallback:{activityId}:{userId}` + 定时扫描 | 轻量 |
| C | 立即重试 3 次 + 最终写入日志告警 | 最简单 |

推荐 **方案 B**（轻量且无需新表）。

### 1.2 死信队列有定义但无消费者

**问题**：`RabbitMQConfig` 已声明 DLX/DLQ，但无 `@RabbitListener` 消费死信。NACK 的消息永久堆积在死信队列中。

**方案**：新增死信消费者用于告警与人工处理：

```java
@Component
@Slf4j
public class SeckillDeadLetterConsumer {

    @RabbitListener(queues = "${seckill.queue-name}.dead")
    public void onDeadLetter(SeckillMessage msg, Message message) {
        log.error("[DEAD-LETTER] 秒杀消息进入死信队列：activityId={}, userId={}, " +
                  "原始消息体={}", msg.getActivityId(), msg.getUserId(),
                  new String(message.getBody()));
        // TODO: 写入告警表 / 发送钉钉/企微通知
    }
}
```

### 1.3 MQ 消费幂等性缺陷

**问题**：`SeckillConsumer` 在 `createOrder()` 成功后，若 ACK 失败（网络闪断），消息重投 → `createOrder()` 因 `uk_user_activity` 唯一约束抛 `DuplicateKeyException` → `catch (Exception e)` 将其当作普通失败 → 写 `"fail"` → 扣回库存 → NACK → **死循环**。

**方案**：区分业务异常与幂等重试：

```java
try {
    orderService.createOrder(order);
    redisTemplate.opsForValue().set(resultKey, order.getId().toString(), 60, TimeUnit.SECONDS);
    channel.basicAck(deliveryTag, false);
} catch (DuplicateKeyException e) {
    // 幂等：订单已存在 → 直接 ACK，不写 fail，不回滚库存
    log.warn("[SeckillConsumer] 重复投递，订单已存在：activityId={}, userId={}",
            msg.getActivityId(), msg.getUserId());
    channel.basicAck(deliveryTag, false);
} catch (Exception e) {
    // 真正的失败 → NACK 进入 DLQ
    log.error("[SeckillConsumer] 建单失败", e);
    redisTemplate.opsForValue().set(resultKey, "fail", 60, TimeUnit.SECONDS);
    redisTemplate.opsForValue().increment(stockKey);
    // 不重新入队，直接拒绝 → 进入 DLQ
    channel.basicReject(deliveryTag, false);
}
```

### 1.4 MQ 消费无退避重试

**问题**：消费者遇到瞬时 DB 故障时立即 NACK → DLQ，应该先重试 N 次。

**方案**：利用消息头 `x-retry-count` 实现退避：

```java
int retryCount = getRetryCount(message);
if (retryCount < 3) {
    // 重新入队，延迟重试
    channel.basicNack(deliveryTag, false, true);  // requeue
    setRetryCount(message, retryCount + 1);
} else {
    // 超过重试次数 → 进入 DLQ
    channel.basicReject(deliveryTag, false);
}
```

> 更好的做法：使用 RabbitMQ Delayed Message Plugin 或 TTL + DLX 实现延迟重试。

### 1.5 Result Key 超时与消费者延迟的竞态

**问题**：result key TTL 为 60s。若消费者 >60s 才完成建单，写入的 orderId 会完整 TTL 60s，但如果消费者在 61s 写入，前一个 key 已过期 → 前端轮询看到 `null` → 返回 `"processing"` → 用户一直等待直到放弃，但订单实际已创建（孤儿订单）。

**方案**：延长 result TTL 到 5 分钟，并增加最终兜底查询：

```java
// SeckillServiceImpl.java — 初始写入
redisTemplate.opsForValue().set(resultKey, "processing", 300, TimeUnit.SECONDS);

// SeckillConsumer.java — 成功后
redisTemplate.opsForValue().set(resultKey, order.getId().toString(), 300, TimeUnit.SECONDS);

// SeckillServiceImpl.getResult() — 兜底查询
if (value == null) {
    // key 过期 → 查 DB 是否有订单（最终一致性兜底）
    SeckillOrder order = orderService.findByActivityAndUser(activityId, userId);
    if (order != null) {
        dto.setStatus("success");
        dto.setOrderId(order.getId());
        return dto;
    }
    dto.setStatus("fail");  // 确实没有 → 失败
    return dto;
}
```

### 1.6 `@EnableScheduling` 已开启但无调度任务

**问题**：注解存在但无 `@Scheduled` 方法。

**建议的调度任务**：

| 任务 | 周期 | 说明 |
|------|------|------|
| Redis 库存 → DB 同步 | 每 30s | 将 `seckill:stock:*` 写回 activity.stock |
| 活动状态自动流转 | 每 5s | `status=0 && now >= startTime` → `status=1` |
| 过期 result key 清理 | 每 60s | 查 DB 补偿孤儿订单 |
| MQ 补偿重投 | 每 30s | 扫描 Redis fallback 队列重投 MQ |

---

## 二、可观测性 (Observability)

### 2.1 无健康检查端点

**问题**：Docker Compose 中 MySQL/Redis/RabbitMQ 有 healthcheck，但 Java 应用没有。

**方案**：引入 Spring Boot Actuator：

```xml
<dependency>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-actuator</artifactId>
</dependency>
```

```yaml
# application.yml
management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,prometheus
  endpoint:
    health:
      show-details: when-authorized
  health:
    redis:
      enabled: true
    rabbit:
      enabled: true
    db:
      enabled: true
```

```yaml
# docker-compose.yml — 后端服务
backend:
  healthcheck:
    test: ["CMD", "curl", "-f", "http://localhost:8080/actuator/health"]
    interval: 15s
    timeout: 5s
    retries: 3
```

### 2.2 无业务指标采集

**问题**：无法知道秒杀成功率、MQ 积压量、平均建单延迟。

**方案**：使用 Micrometer + Prometheus（Actuator 自带）：

```java
// SeckillMetrics.java — 自定义指标
@Component
public class SeckillMetrics {
    private final MeterRegistry registry;

    public SeckillMetrics(MeterRegistry registry) {
        this.registry = registry;
    }

    public void recordSeckill(String result) {
        Counter.builder("seckill.requests.total")
                .tag("result", result)
                .register(registry)
                .increment();
    }

    public void recordOrderLatency(long millis) {
        Timer.builder("seckill.order.create.latency")
                .register(registry)
                .record(millis, TimeUnit.MILLISECONDS);
    }
}
```

### 2.3 无结构化日志 / Trace ID

**问题**：日志无关联 ID，一次秒杀请求跨越 Controller → Service → MQ Producer → MQ Consumer，无法串联追踪。

**方案**：用 MDC 传递 traceId：

```java
// TraceIdFilter.java
@Component
public class TraceIdFilter extends OncePerRequestFilter {
    @Override
    protected void doFilterInternal(HttpServletRequest request, ...) {
        String traceId = request.getHeader("X-Trace-Id");
        if (traceId == null) traceId = UUID.randomUUID().toString().substring(0, 8);
        MDC.put("traceId", traceId);
        response.setHeader("X-Trace-Id", traceId);
        try {
            chain.doFilter(request, response);
        } finally {
            MDC.clear();
        }
    }
}
```

```yaml
# application.yml — logback 使用 traceId
logging:
  pattern:
    console: "%d{HH:mm:ss} [%thread] [%X{traceId}] %-5level %logger{36} - %msg%n"
```

MQ 消息体中加入 `traceId`，消费者从消息中恢复 MDC。

### 2.4 管理后台 → 客户端统计

**问题**：`DashboardView.vue` 请求全部订单（可能上千条）在客户端计算 revenue 总和。

**方案**：新增后端聚合端点：

```java
@GetMapping("/api/admin/stats")
@PreAuthorize("hasRole('ADMIN')")
public Result<DashboardStats> getStats() {
    return Result.success(dashboardService.getStats());
}
```

```sql
-- DashboardStats 直接 SQL 聚合，无需加载全部数据
SELECT
    COUNT(*) AS totalOrders,
    COALESCE(SUM(price), 0) AS totalRevenue
FROM seckill_order
WHERE status = 1;
```

---

## 三、数据一致性 (Data Integrity)

### 3.1 Redis 库存与 DB 库存不同步

**问题**：创建活动时初始化 Redis 库存，秒杀完成后 Redis 库存不再写回 DB。若 Redis 重启/淘汰，库存数据丢失。

**方案**：定时同步 + 启动回灌：

```java
// StockSyncScheduler.java
@Scheduled(fixedRate = 30_000)
public void syncStockToDB() {
    Set<String> keys = redisTemplate.keys(stockKeyPrefix + "*");
    for (String key : keys) {
        Long activityId = Long.parseLong(key.replace(stockKeyPrefix, ""));
        Object stock = redisTemplate.opsForValue().get(key);
        if (stock != null) {
            activityMapper.updateStock(activityId, Integer.parseInt(stock.toString()));
        }
    }
}
```

```java
// 应用启动时 — ActivityServiceImpl
@PostConstruct
public void restoreStockFromDB() {
    List<SeckillActivity> activities = activityMapper.selectList(
        new LambdaQueryWrapper<SeckillActivity>().eq(SeckillActivity::getStatus, 1));
    for (SeckillActivity a : activities) {
        String stockKey = stockKeyPrefix + a.getId();
        if (Boolean.FALSE.equals(redisTemplate.hasKey(stockKey))) {
            redisTemplate.opsForValue().set(stockKey, a.getStock().toString());
            log.info("[StockRestore] 恢复活动 {} 库存到 Redis: {}", a.getId(), a.getStock());
        }
    }
}
```

### 3.2 秒杀页面显示 DB 库存而非 Redis 库存

**问题**：`SeckillView.vue` 显示 `activity.stock`（来自 DB），但真实库存是 Redis `seckill:stock:{id}`。页面显示的库存数是滞后的。

**方案**：`ActivityServiceImpl.getActivity()` 同步返回 Redis 实时库存：

```java
@Override
public SeckillActivity getActivity(Long id) {
    SeckillActivity activity = activityMapper.selectById(id);
    if (activity == null) throw new BusinessException(ErrorCode.ACTIVITY_NOT_FOUND);
    if (activity.getStatus() == 1) {
        String stockKey = stockKeyPrefix + id;
        Object stock = redisTemplate.opsForValue().get(stockKey);
        if (stock != null) {
            activity.setStock(Integer.parseInt(stock.toString()));
        }
    }
    return activity;
}
```

### 3.3 SeckillController 吞异常 → 前端无感知

**问题**：`SeckillController.doSeckill()` 不 catch 异常，依赖 `GlobalExceptionHandler`。但 `BusinessException` 的响应 code 不是 200，前端 axios 拦截器 `if (data.code === 200)` 不通过 → 进入 reject → 只显示错误消息。秒杀失败（库存不足/重复购买）虽然正确提示，但前端 `handleSeckill` 的 `catch` 块什么都不做——错误信息在拦截器里已经 `ElMessage.error` 了，所以**看起来是工作正常的**。

> **重新评估**：这个其实是正确的行为。GlobalExceptionHandler 返回 `{code: 3004, message: "库存不足"}` → axios 拦截器 `data.code !== 200` → `ElMessage.error("库存不足")` → `Promise.reject(data)` → `handleSeckill` catch 块空处理。消息已正确显示给用户。

这条移除 —— 当前实现是正确的。

### 3.4 注册响应泄露密码字段

**问题**：`UserController.register()` 返回 `Result<User>`。`UserServiceImpl.register()` 已 `user.setPassword(null)`，但依赖代码规范保证。若未来有人新增直接返回 `userMapper.selectById()` 的路径，就会泄露。

**方案**：用 DTO 而非 Entity 做响应：

```java
// UserDTO.java
@Data
public class UserDTO {
    private Long id;
    private String username;
    private String email;
    private String role;
}

// UserController.register()
@PostMapping("/register")
public Result<UserDTO> register(@Valid @RequestBody RegisterRequest req) {
    User user = userService.register(req);
    UserDTO dto = new UserDTO();
    BeanUtils.copyProperties(user, dto);
    return Result.success(dto);
}
```

---

## 四、API 设计深化

### 4.1 管理后台列表无分页

| 端点 | 当前行为 | 风险 |
|------|---------|------|
| `GET /api/admin/orders` | 返回全量 | 1000+ 条时响应 >5MB |
| `GET /api/activities` | 返回全量 | 尚可，但无上限 |

**方案**：

```java
// 统一分页响应
@Data
public class PageResult<T> {
    private List<T> records;
    private long total;
    private int page;
    private int size;
}
```

```java
@GetMapping("/api/admin/orders")
@PreAuthorize("hasRole('ADMIN')")
public Result<PageResult<SeckillOrder>> listAllOrders(
        @RequestParam(defaultValue = "1") int page,
        @RequestParam(defaultValue = "20") int size) {
    return Result.success(orderService.listAllOrders(page, size));
}
```

前端同步改为分页表格。

### 4.2 Admin 增/改接受裸 Entity 无校验

**问题**：`createProduct(@RequestBody Product product)` 直接接受 Entity。客户可发送 `createdAt: "2099-01-01"` 等非预期字段。

**方案**：区分 DTO 与 Entity：

```
Request DTO → Controller → Service → Entity → Mapper → DB
Response DTO ← Controller ← Service ← Entity ← Mapper ← DB
```

```java
// 为每个管理操作建立 DTO
@Data
public class CreateProductRequest {
    @NotBlank @Size(max=128) private String name;
    @NotNull @DecimalMin("0.01") private BigDecimal price;
    @Min(0) private Integer stock;
    @Size(max=2000) private String description;
    @Size(max=512) private String imageUrl;
    @Min(0) @Max(1) private Integer status;
}
```

### 4.3 无请求体大小限制

```yaml
spring:
  servlet:
    multipart:
      max-file-size: 5MB
      max-request-size: 5MB

server:
  max-http-request-header-size: 8KB
```

---

## 五、后端性能深化

### 5.1 活动列表无缓存

**问题**：`GET /api/activities` 每次查 DB。活动列表是高频访问的热点。

**方案**：Redis 缓存 + 写时失效：

```java
@Cacheable(value = "activities", unless = "#result.isEmpty()")
public List<SeckillActivity> listActivities() { ... }

@CacheEvict(value = "activities", allEntries = true)
public SeckillActivity createActivity(SeckillActivity activity) { ... }
```

### 5.2 商品列表分页无上限保护

```java
// ProductController.listProducts()
int safeSize = Math.min(size, 100); // 最大 100 条/页
```

### 5.3 DB 连接池未调优

```yaml
spring:
  datasource:
    hikari:
      maximum-pool-size: 30
      minimum-idle: 5
      connection-timeout: 3000
      idle-timeout: 600000
      max-lifetime: 1800000
```

### 5.4 RabbitMQ 消费并发度

```yaml
spring:
  rabbitmq:
    listener:
      simple:
        concurrency: 5       # 5 个消费者线程
        max-concurrency: 10  # 峰值 10
        prefetch: 10         # 每个消费者一次取 10 条
```

---

## 六、前端体验深化

### 6.1 管理后台订单无分页

**问题**：`OrderManageView.vue` 调用 `orderApi.allOrders()` 返回全量。

**方案**：增加分页组件，API 支持 `?page=&size=`。

### 6.2 仪表盘统计全量拉取

**问题**：`DashboardView.vue` 拉取全部订单 + 全部商品在客户端求和。

**方案**：新增 `GET /api/admin/stats` 后端聚合端点（见 2.4）。

### 6.3 无离线检测

```javascript
// utils/network.js
import { ref } from 'vue'

export function useOnline() {
  const online = ref(navigator.onLine)
  window.addEventListener('online', () => online.value = true)
  window.addEventListener('offline', () => online.value = false)
  return online
}
```

在 NavBar 或全局显示离线状态条。

### 6.4 表单离开保护

Admin 编辑弹窗打开时，未保存就关闭 → 静默丢失数据。

```javascript
// 在 dialog 的 before-close 钩子
const beforeClose = (done) => {
  if (hasUnsavedChanges.value) {
    ElMessageBox.confirm('有未保存的修改，确定关闭？')
      .then(() => done())
      .catch(() => {})
  } else {
    done()
  }
}
```

### 6.5 可访问性 (Accessibility)

当前缺少：
- `aria-label` 在无文本按钮上（如纯图标按钮）
- 焦点管理（弹窗打开时光标未聚焦到弹窗内）
- 颜色对比度（部分灰色文本对比度不足）
- `role="alert"` 在错误提示上

```vue
<el-button aria-label="关闭对话框" @click="dialogVisible = false">
  <el-icon><Close /></el-icon>
</el-button>
```

### 6.6 键盘快捷键

| 快捷键 | 行为 |
|--------|------|
| `Esc` | 关闭当前弹窗 / 取消编辑 |
| `Enter` | 确认提交（dialog 内） |
| `Ctrl+K` | 聚焦搜索框 |

### 6.7 暗色模式完善

当前无暗色模式。Element Plus 2.x 已内置 Dark Mode：

```javascript
// main.js
import 'element-plus/theme-chalk/dark/css-vars.css'
```

```vue
<!-- NavBar.vue -->
<el-switch v-model="isDark" @change="toggleDark" />
```

```javascript
import { useDark, useToggle } from '@vueuse/core'
const isDark = useDark()
const toggleDark = useToggle(isDark)
```

---

## 七、测试体系

### 7.1 现状

`src/test/java/com/example/seckill/` 目录为空 — **零测试**。

### 7.2 分层测试策略

| 层级 | 工具 | 覆盖目标 | 关键用例 |
|------|------|---------|---------|
| 单元测试 | JUnit 5 + Mockito | Service 逻辑 | `UserServiceImpl.register()` 用户名重复 |
| 集成测试 | SpringBootTest + Testcontainers | Redis + MQ 流程 | 秒杀完整链路（Lua 扣减 → MQ → 建单） |
| API 测试 | MockMvc | Controller 层 | 登录/注册/秒杀 正常/异常路径 |
| E2E | Playwright (前端) | 用户操作流程 | 登录 → 浏览商品 → 参与秒杀 → 查看订单 |

### 7.3 秒杀核心链路测试（最关键）

```java
@SpringBootTest
@Testcontainers
class SeckillIntegrationTest {

    @Container
    static GenericContainer<?> redis = new GenericContainer<>("redis:7-alpine")
            .withExposedPorts(6379);

    @Container
    static GenericContainer<?> rabbitmq = new GenericContainer<>("rabbitmq:3-management-alpine")
            .withExposedPorts(5672);

    @Test
    void shouldDeductStockAtomically_ConcurrentRequests() {
        // 100 个用户同时秒杀 50 库存活动
        // 验证：恰好 50 个成功，50 个失败
    }

    @Test
    void shouldCreateOrderAfterMqConsumed() {
        // 秒杀成功 → 等待 MQ 消费 → 验证订单已落库
    }
}
```

### 7.4 CI/CD

建议 `.github/workflows/ci.yml`：

```yaml
name: CI
on: [push, pull_request]
jobs:
  test:
    runs-on: ubuntu-latest
    services:
      mysql:
        image: mysql:8.0
        env:
          MYSQL_DATABASE: seckill_test
          MYSQL_ROOT_PASSWORD: test
        ports: ['3306:3306']
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with: { java-version: '17', distribution: 'temurin' }
      - run: cd backend && mvn verify
      - uses: actions/setup-node@v4
        with: { node-version: '20' }
      - run: cd frontend && npm ci && npm run build
```

---

## 八、运维就绪 (Operations)

### 8.1 优雅停机

```yaml
server:
  shutdown: graceful

spring:
  lifecycle:
    timeout-per-shutdown-phase: 30s
```

### 8.2 Docker Compose 后端 healthcheck

```yaml
services:
  backend:
    build: ./backend
    ports:
      - "8080:8080"
    depends_on:
      mysql:
        condition: service_healthy
      redis:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "curl -f http://localhost:8080/actuator/health || exit 1"]
      interval: 15s
      timeout: 5s
      retries: 5
      start_period: 40s
```

### 8.3 日志管理

```yaml
logging:
  file:
    path: /var/log/seckill
  logback:
    rollingpolicy:
      max-file-size: 10MB
      max-history: 30
      total-size-cap: 1GB
```

### 8.4 默认 Profile 与多环境

```yaml
# application.yml — 通用配置
spring:
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:dev}
```

创建 `application-prod.yml`：
```yaml
spring:
  datasource:
    url: ${DB_URL}
    username: ${DB_USER}
    password: ${DB_PASSWORD}

mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.nologging.NoLoggingImpl

knife4j:
  enable: false
```

---

## 九、安全纵深

### 9.1 Swagger/Knife4j 生产暴露

```java
// Knife4jConfig.java
@Profile("!prod")  // 仅非生产环境启用
@Configuration
public class Knife4jConfig { ... }
```

### 9.2 HTTPS 强制

```yaml
# application-prod.yml
server:
  ssl:
    key-store: classpath:keystore.p12
    key-store-password: ${SSL_KEYSTORE_PASSWORD}
    key-store-type: PKCS12

security:
  require-ssl: true
```

### 9.3 安全响应头补充

```java
.headers(h -> h
    // ... 已有配置 ...
    .xssProtection(xss -> xss.headerValue(XXssProtectionHeaderWriter.HeaderValue.ENABLED_MODE_BLOCK))
    .contentTypeOptions(Customizer.withDefaults())
    .hsts(hsts -> hsts
        .includeSubDomains(true)
        .maxAgeInSeconds(31536000))
)
```

### 9.4 并发注册竞态

```java
// UserServiceImpl.register() — 加分布式锁防止并发同名注册
String lockKey = "lock:register:" + req.getUsername();
Boolean locked = redisTemplate.opsForValue()
        .setIfAbsent(lockKey, "1", 5, TimeUnit.SECONDS);
if (Boolean.FALSE.equals(locked)) {
    throw new BusinessException(429, "请勿重复提交");
}
try {
    // 原有注册逻辑
} finally {
    redisTemplate.delete(lockKey);
}
```

---

## 十、优化优先级总表

| 优先级 | 编号 | 事项 | 风险等级 | 工时 |
|--------|------|------|---------|------|
| **P0** | 1.1 | MQ 发布确认 + 补偿 | 🔴 数据丢失 | 3h |
| **P0** | 1.3 | MQ 消费幂等性修复 | 🔴 超卖/死循环 | 1h |
| **P0** | 1.5 | Result key 超时兜底 | 🔴 孤儿订单 | 1h |
| **P0** | 3.1 | Redis → DB 库存同步 | 🔴 数据丢失 | 2h |
| **P1** | 1.2 | 死信消费者 | 🔴 消息堆积无感知 | 0.5h |
| **P1** | 2.1 | 健康检查端点 | ⭐⭐ 运维盲区 | 1h |
| **P1** | 2.4 | 后端聚合统计端点 | ⭐⭐ 性能+数据量 | 1h |
| **P1** | 3.2 | 秒杀页显示 Redis 实时库存 | ⭐⭐ 信息不准 | 0.3h |
| **P1** | 4.1 | 管理后台分页 | ⭐⭐ 性能/带宽 | 2h |
| **P1** | 4.2 | Admin DTO 层 | ⭐⭐ 字段注入风险 | 3h |
| **P1** | 9.1 | Swagger 生产禁用 | 🔴 信息泄露 | 0.2h |
| **P2** | 1.4 | MQ 退避重试 | ⭐ 瞬时故障恢复 | 1.5h |
| **P2** | 1.6 | 调度任务实现 | ⭐ 自动化运维 | 3h |
| **P2** | 2.2 | 业务指标采集 | ⭐ 可观测性 | 2h |
| **P2** | 2.3 | Trace ID 全链路 | ⭐ 可观测性 | 2h |
| **P2** | 5.1 | 活动列表缓存 | ⭐ 性能 | 0.5h |
| **P2** | 5.3+5.4 | 连接池 + MQ 并发调优 | ⭐ 吞吐量 | 0.5h |
| **P2** | 6.3+6.4 | 离线检测 + 表单保护 | ⭐ 体验 | 1h |
| **P2** | 6.7 | 暗色模式 | ⭐ 体验 | 2h |
| **P2** | 7.2 | 秒杀核心链路测试 | ⭐⭐ 质量保障 | 4h |
| **P2** | 8.1+8.2 | 优雅停机 + Docker 健康检查 | ⭐ 运维 | 1h |
| **P3** | 3.4 | 注册响应 DTO | ⭐ 防御性 | 1h |
| **P3** | 4.3 | 请求体大小限制 | ⭐ 安全 | 0.2h |
| **P3** | 6.5 | 可访问性 | ⭐ 合规 | 2h |
| **P3** | 6.6 | 键盘快捷键 | ⭐ 效率 | 1h |
| **P3** | 7.4 | CI/CD Pipeline | ⭐ 自动化 | 2h |
| **P3** | 8.3+8.4 | 日志轮转 + 多环境 | ⭐ 运维 | 1h |
| **P3** | 9.2+9.3 | HTTPS + 安全头补充 | ⭐ 安全 | 1h |
| **P3** | 9.4 | 注册防重分布式锁 | ⭐ 安全 | 0.5h |

---

## V1 vs V2 对比

| 维度 | V1 优化 | V2 优化 |
|------|---------|---------|
| **层次** | 应用表层（UI/配置/过滤器） | 架构深层（消息可靠性/数据一致性） |
| **安全性** | XSS/CSRF/CORS/Cookie | HTTPS/生产锁定/Swagger 禁用 |
| **可靠性** | 基本错误处理 | MQ 确认/补偿/死信/幂等 |
| **可观测性** | 审计日志 | 健康检查/指标/Trace ID |
| **性能** | shallowRef/懒加载 | 缓存/连接池/分页 |
| **运维** | 环境变量 | 优雅停机/Docker 健康检查/日志轮转 |
| **质量** | — | 测试体系/CI/CD |
